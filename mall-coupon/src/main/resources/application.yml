spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.92.128:3306/mall_sms
    username: root
    password: liukaiying1314
  mvc:
    servlet:
      load-on-startup: 1
  cloud:
    nacos:
      config:
        refresh-enabled: true
        file-extension: yml
      discovery:
        server-addr: 192.168.92.128:8848
  application:
    name: mall-coupon

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 9900